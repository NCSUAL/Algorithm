WITH 
O_G AS (
    SELECT
    ID
    FROM ECOLI_DATA
    WHERE PARENT_ID IS NULL 
),
T_G AS(
 SELECT
 E.ID
 FROM ECOLI_DATA E
 LEFT JOIN O_G O
 ON E.PARENT_ID = O.ID
 WHERE O.ID IS NOT NULL
)


SELECT
E.ID
FROM ECOLI_DATA E
LEFT JOIN T_G T
ON E.PARENT_ID = T.ID
WHERE T.ID IS NOT NULL
